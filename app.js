document.addEventListener("DOMContentLoaded", function () {
  let deferredPrompt;

  function applyStyle() {
    document.body.style.backgroundColor =
      document.body.style.backgroundColor === "lightblue"
        ? "white"
        : "lightblue";
    console.log("تمم تغير لون الخلفية!");
  }


});
